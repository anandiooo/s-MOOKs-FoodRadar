{
  "name": "mooks-content",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MOOKS content pipeline — authored drops in, validated static JSON out. No server.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node test/wrap.test.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "node scripts/validate.mjs && node scripts/build.mjs",
    "ci": "npm run test && npm run build"
  }
}
